preprocessing：数据预处理，将txt数据转换为excel数据，并切割成静息状态数据与实验状态数据

demo_2：计算EEG和EDA的调制指数（MI）。运用的是EEG和EDA的相位-幅度耦合分析方法。采用实验状态1分钟的切割方式，并计算了不同分箱数下的MI值。
一般默认的分箱数为36，是根据文献所得。

demo_3：绘制不同ID下，静息状态和实验状态的MI值（EDA→EEG）的对比图。

demo_4:假设检验部分（EDA→EEG），探究静息状态与实验状态的MI值差异是否显著。

demo_5:构建5种生理信号的PAC网络，并判断是否高于随机水平，判断MI值这条边是否存在。

demo_6:平均向量长度的方法

demo_7:平均向量长度的方法，只计算PAC网络，并且保存每个子网络的网络特征。其中有：密度、平均强度、节点的度中心性、模块度、全局聚集系数
PAC:为封装好的类，只需要调用即可。

demo_8：平均向量长度的方法，只计算PPC网络，并且保存每个子网络的网络特征。其中有：密度、平均强度、节点的度中心性、模块度、全局聚集系数


demo_9：平均向量长度的方法，读取pac_matrices_exp.npy文件，

differential analysis：平均向量长度的方法，统计实验一和实验三不同的网络特征的差异，并绘制箱型图。并将其存放在output.xlsx这个表格里面

demo_1：探究复杂网络动态特征
全局特征：研究两个实验条件下有向网络的整体结构差异，揭示任务复杂度对整体网络属性的影响。
时间相关性特征：分析有向网络的动态变化模式，探讨实验任务的时间演化对耦合网络的调控。

demo_11：绘制时间片段的相位振幅图。

demo_12：原版PAC_CUDA，切片希尔伯特，切片计算得到网络。重新找到的。

save_networks：单独切片，单独统计构建有向网络，主要存放为CPU跑的npy网络文件。
save_networks_all:单独切片，实验阶段数据统计all（0.25）片段耦合值进行有向网络的构建，以及采用FDR校正。静息状态阶段boferroni校正。计算p值时随机采样为100次
save_networks_pt：单独切片，单独统计构建有向网络，主要存放为GPU跑的pt网络文件。